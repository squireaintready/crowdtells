/** The single mobile/phone breakpoint, shared by every JS `matchMedia` reader so a
 * change happens in one place (the CSS files mirror this value in their own media
 * queries — keep them in sync). */
export const MOBILE_MQ = '(max-width: 560px)';
